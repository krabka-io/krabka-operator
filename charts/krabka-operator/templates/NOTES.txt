krabka-operator is installed.

The operator is watching {{ if .Values.watchNamespaces }}namespaces: {{ join "," .Values.watchNamespaces }}{{ else }}all namespaces (cluster-scoped){{ end }}.

This chart installed the CRDs from its own crds/ directory. Helm creates them
on install and leaves them alone on upgrade. Apply them by hand after a chart
upgrade that changes a schema:

  kubectl apply -f charts/krabka-operator/crds

Create a placeholder cluster:

  cat <<EOF | kubectl apply -f -
  apiVersion: krabka.io/v1alpha1
  kind: Kafka
  metadata:
    name: demo
  spec:
    kafkaVersion: "0.1.1"
  EOF
